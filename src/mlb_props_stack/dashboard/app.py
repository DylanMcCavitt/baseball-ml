"""Placeholder dashboard entrypoint for a future Streamlit app."""


def build_dashboard_banner() -> str:
    """Return a stub banner until the Streamlit UI lands."""
    return (
        "Dashboard placeholder: use this module for the future Streamlit app "
        "once tracking views are ready."
    )


def main() -> None:
    print(build_dashboard_banner())


if __name__ == "__main__":
    main()
